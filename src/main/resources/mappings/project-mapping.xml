<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ProjectDAO">

    <!-- 프로젝트 목록 -->
    <select id="getProjectList" resultType="Project">
        select project.projectName, projectgroup.position, projectgroup.role, project.progress
        from projectgroup
            join project
            on projectgroup.projectId = project.projectId
        where projectgroup.userId = #{userId}
    </select>
</mapper>